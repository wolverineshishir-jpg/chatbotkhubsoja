type PageHeaderProps = {
  eyebrow?: string;
  title?: string;
  description: string;
};

export function PageHeader({ eyebrow, title, description }: PageHeaderProps) {
  void eyebrow;
  void title;
  void description;
  return null;
}
